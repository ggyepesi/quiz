package quiz.ui;

/**
 * Selection behavior owned by a ChoiceBoard. EXTERNAL lets a quiz evaluator
 * decide whether and how a click changes state.
 */
public enum ChoiceSelectionMode {
    EXTERNAL,
    SINGLE,
    MULTIPLE
}
