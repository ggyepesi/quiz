/**
 * Standalone {@code main} entry points and tools that exercise the explore
 * pipeline (e.g. building a constellation snapshot, blurring chart text) —
 * useful for manual runs and verification, not part of the app's UI flow.
 */
package wikidata.explore.demo;
