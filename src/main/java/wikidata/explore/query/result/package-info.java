/**
 * Typed query results returned by {@code query.logical} queries:
 * {@code TableQueryResult} (columns + rows, for search/discovery tables),
 * {@code DiscoveredProperty} (a profiled property), and {@code ObjectQueryResult}
 * (generated object graphs).
 */
package wikidata.explore.query.result;
