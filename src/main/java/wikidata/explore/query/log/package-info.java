/**
 * The query-log model behind the ModelBuilder's log window:
 * {@code WorkflowRecorder} captures a tree of {@code LogNode}/{@code LogStep}
 * entries (request, status, timing, summary) that {@code LogListener}s render.
 */
package wikidata.explore.query.log;
