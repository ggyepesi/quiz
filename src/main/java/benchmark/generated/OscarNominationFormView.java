package benchmark.generated;

import oscar.OscarNomination;
import javax.swing.*;
import java.awt.*;

/**
 * Automatically generated by FormStamper.
 * Specific compilation layout for: OscarNomination
 * Config Constraints: NOT_IMAGE_PANE_FIELDS
 */
public final class OscarNominationFormView extends JPanel {

    public OscarNominationFormView(OscarNomination data) {
        // Step 1: Base panel initialization
        this.setLayout(new GridBagLayout());
        this.setOpaque(false);
        this.setBorder(BorderFactory.createLineBorder(Color.LIGHT_GRAY, 1));

        if (data == null) return;

        int mainRow = 0;

        // =================================================================
        // 1. Structural Composite Panel: nominee
        // =================================================================
        if (data.getNominee() != null) {
            JPanel nomineePanel = new JPanel(new GridBagLayout());
            nomineePanel.setOpaque(false);
            nomineePanel.setBorder(BorderFactory.createTitledBorder("nominee"));

            GridBagConstraints subGbc = new GridBagConstraints();
            subGbc.fill = GridBagConstraints.HORIZONTAL;
            subGbc.weightx = 1.0;
            int subRow = 0;

            // Property: label (nominee.name)
            if (data.getNominee().getName() != null && !data.getNominee().getName().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                nomineePanel.add(createValuePanel("nominee.name", data.getNominee().getName()), subGbc);
            }

            // Property: qid (nominee.qid)
            if (data.getNominee().getQid() != null && !data.getNominee().getQid().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                nomineePanel.add(createValuePanel("nominee.qid", data.getNominee().getQid()), subGbc);
            }

            // Property: url (nominee.url)
            if (data.getNominee().getQid() != null && !data.getNominee().getQid().isEmpty()) {
                subGbc.gridy = subRow++;
                String urlVal = "https://www.wikidata.org/wiki/" + data.getNominee().getQid();
                nomineePanel.add(createValuePanel("nominee.url", urlVal), subGbc);
            }

            // Only add the parent sub-panel if it actually contains inner components
            if (subRow > 0) {
                this.add(nomineePanel, layoutConstraints(mainRow++));
            }
        }

        // =================================================================
        // 2. Structural Composite Panel: award
        // =================================================================
        if (data.getAward() != null) {
            JPanel awardPanel = new JPanel(new GridBagLayout());
            awardPanel.setOpaque(false);
            awardPanel.setBorder(BorderFactory.createTitledBorder("award"));

            GridBagConstraints subGbc = new GridBagConstraints();
            subGbc.fill = GridBagConstraints.HORIZONTAL;
            subGbc.weightx = 1.0;
            int subRow = 0;

            if (data.getAward().getName() != null && !data.getAward().getName().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                awardPanel.add(createValuePanel("award.name", data.getAward().getName()), subGbc);
            }

            if (data.getAward().getQid() != null && !data.getAward().getQid().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                awardPanel.add(createValuePanel("award.qid", data.getAward().getQid()), subGbc);
            }

            if (data.getAward().getQid() != null && !data.getAward().getQid().isEmpty()) {
                subGbc.gridy = subRow++;
                String urlVal = "https://www.wikidata.org/wiki/" + data.getAward().getQid();
                awardPanel.add(createValuePanel("award.url", urlVal), subGbc);
            }

            if (subRow > 0) {
                this.add(awardPanel, layoutConstraints(mainRow++));
            }
        }

        // =================================================================
        // 3. Structural Composite Panel: work
        // =================================================================
        if (data.getWork() != null) {
            JPanel workPanel = new JPanel(new GridBagLayout());
            workPanel.setOpaque(false);
            workPanel.setBorder(BorderFactory.createTitledBorder("work"));

            GridBagConstraints subGbc = new GridBagConstraints();
            subGbc.fill = GridBagConstraints.HORIZONTAL;
            subGbc.weightx = 1.0;
            int subRow = 0;

            if (data.getWork().getName() != null && !data.getWork().getName().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                workPanel.add(createValuePanel("work.name", data.getWork().getName()), subGbc);
            }

            if (data.getWork().getQid() != null && !data.getWork().getQid().trim().isEmpty()) {
                subGbc.gridy = subRow++;
                workPanel.add(createValuePanel("work.qid", data.getWork().getQid()), subGbc);
            }

            if (data.getWork().getQid() != null && !data.getWork().getQid().isEmpty()) {
                subGbc.gridy = subRow++;
                String urlVal = "https://www.wikidata.org/wiki/" + data.getWork().getQid();
                workPanel.add(createValuePanel("work.url", urlVal), subGbc);
            }

            if (subRow > 0) {
                this.add(workPanel, layoutConstraints(mainRow++));
            }
        }

        // =================================================================
        // 4. Primitive Fields
        // =================================================================

        // Field: ceremonyYear
        if (data.getCeremonyYear() != 0) {
            this.add(createValuePanel("ceremonyYear", String.valueOf(data.getCeremonyYear())), layoutConstraints(mainRow++));
        }

        // Field: filmYear
        if (data.getFilmYear() != 0) {
            this.add(createValuePanel("filmYear", String.valueOf(data.getFilmYear())), layoutConstraints(mainRow++));
        }

        // Field: winner
        this.add(createValuePanel("winner", String.valueOf(data.isWinner())), layoutConstraints(mainRow++));
    }

    private JPanel createValuePanel(String title, String value) {
        JPanel p = new JPanel(new BorderLayout());
        p.setOpaque(false);
        p.setBorder(BorderFactory.createTitledBorder(title));
        JLabel label = new JLabel(value);
        label.setForeground(new Color(0, 80, 180));
        p.add(label, BorderLayout.WEST);
        return p;
    }

    private GridBagConstraints layoutConstraints(int y) {
        GridBagConstraints gbc = new GridBagConstraints();
        gbc.gridy = y;
        gbc.fill = GridBagConstraints.HORIZONTAL;
        gbc.weightx = 1.0;
        gbc.insets = new Insets(4, 4, 4, 4);
        return gbc;
    }
}